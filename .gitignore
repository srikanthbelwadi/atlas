__pycache__/
*.pyc
.env
.DS_Store
node_modules/
.next/

# Python virtualenvs (e.g. infra/functions/on_user_created/venv) — never commit these,
# they're machine-specific and huge. Missed when the venv was first created; fixing now.
venv/
.venv/
