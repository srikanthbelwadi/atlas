# Copy to .env.local for local builds. In deployment these are set via
# apphosting.yaml (plain values) and `firebase apphosting:secrets:set`
# (the two marked secret there) — never commit a real .env.local.

NEXT_PUBLIC_ATLAS_API_BASE_URL=https://atlas-orchestrator-REPLACE.run.app
NEXT_PUBLIC_FIREBASE_API_KEY=replace-me
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=atlas-ard-okf.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=atlas-ard-okf
NEXT_PUBLIC_FIREBASE_APP_ID=replace-me
